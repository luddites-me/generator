---
platform: linux

image_resource:
  type: registry-image
  source: { repository: debian, tag: buster }

inputs:
  - name: ns8-<%- platformName %>-platform

run:
  path: /bin/sh
  args:
    - -c
    - |
      apt-get update

      # TODO: can we use scripts from "${repoRoot}/build/"?
